# GENERATED by scripts/mac_roman.py — do not edit by hand.
#
# The Macintosh Roman high half, 0x80..0xFF, as (code, code point) pairs taken from
# Python's own `mac_roman` codec and from nothing in rekha. programs/name_test.cyr
# drives rekha's decoder with these, so a transcription slip in src/name.cyr's
# chunks shows up as a failed check rather than as one wrong character in a name.

var REKHA_MR_VEC_N = 128;

# The expected code point for Mac Roman byte 0x80 + i.
fn rekha_mr_vec(i): i64 {
    if (i ==   0) { return   196; }   # 0x80
    if (i ==   1) { return   197; }   # 0x81
    if (i ==   2) { return   199; }   # 0x82
    if (i ==   3) { return   201; }   # 0x83
    if (i ==   4) { return   209; }   # 0x84
    if (i ==   5) { return   214; }   # 0x85
    if (i ==   6) { return   220; }   # 0x86
    if (i ==   7) { return   225; }   # 0x87
    if (i ==   8) { return   224; }   # 0x88
    if (i ==   9) { return   226; }   # 0x89
    if (i ==  10) { return   228; }   # 0x8A
    if (i ==  11) { return   227; }   # 0x8B
    if (i ==  12) { return   229; }   # 0x8C
    if (i ==  13) { return   231; }   # 0x8D
    if (i ==  14) { return   233; }   # 0x8E
    if (i ==  15) { return   232; }   # 0x8F
    if (i ==  16) { return   234; }   # 0x90
    if (i ==  17) { return   235; }   # 0x91
    if (i ==  18) { return   237; }   # 0x92
    if (i ==  19) { return   236; }   # 0x93
    if (i ==  20) { return   238; }   # 0x94
    if (i ==  21) { return   239; }   # 0x95
    if (i ==  22) { return   241; }   # 0x96
    if (i ==  23) { return   243; }   # 0x97
    if (i ==  24) { return   242; }   # 0x98
    if (i ==  25) { return   244; }   # 0x99
    if (i ==  26) { return   246; }   # 0x9A
    if (i ==  27) { return   245; }   # 0x9B
    if (i ==  28) { return   250; }   # 0x9C
    if (i ==  29) { return   249; }   # 0x9D
    if (i ==  30) { return   251; }   # 0x9E
    if (i ==  31) { return   252; }   # 0x9F
    if (i ==  32) { return  8224; }   # 0xA0
    if (i ==  33) { return   176; }   # 0xA1
    if (i ==  34) { return   162; }   # 0xA2
    if (i ==  35) { return   163; }   # 0xA3
    if (i ==  36) { return   167; }   # 0xA4
    if (i ==  37) { return  8226; }   # 0xA5
    if (i ==  38) { return   182; }   # 0xA6
    if (i ==  39) { return   223; }   # 0xA7
    if (i ==  40) { return   174; }   # 0xA8
    if (i ==  41) { return   169; }   # 0xA9
    if (i ==  42) { return  8482; }   # 0xAA
    if (i ==  43) { return   180; }   # 0xAB
    if (i ==  44) { return   168; }   # 0xAC
    if (i ==  45) { return  8800; }   # 0xAD
    if (i ==  46) { return   198; }   # 0xAE
    if (i ==  47) { return   216; }   # 0xAF
    if (i ==  48) { return  8734; }   # 0xB0
    if (i ==  49) { return   177; }   # 0xB1
    if (i ==  50) { return  8804; }   # 0xB2
    if (i ==  51) { return  8805; }   # 0xB3
    if (i ==  52) { return   165; }   # 0xB4
    if (i ==  53) { return   181; }   # 0xB5
    if (i ==  54) { return  8706; }   # 0xB6
    if (i ==  55) { return  8721; }   # 0xB7
    if (i ==  56) { return  8719; }   # 0xB8
    if (i ==  57) { return   960; }   # 0xB9
    if (i ==  58) { return  8747; }   # 0xBA
    if (i ==  59) { return   170; }   # 0xBB
    if (i ==  60) { return   186; }   # 0xBC
    if (i ==  61) { return   937; }   # 0xBD
    if (i ==  62) { return   230; }   # 0xBE
    if (i ==  63) { return   248; }   # 0xBF
    if (i ==  64) { return   191; }   # 0xC0
    if (i ==  65) { return   161; }   # 0xC1
    if (i ==  66) { return   172; }   # 0xC2
    if (i ==  67) { return  8730; }   # 0xC3
    if (i ==  68) { return   402; }   # 0xC4
    if (i ==  69) { return  8776; }   # 0xC5
    if (i ==  70) { return  8710; }   # 0xC6
    if (i ==  71) { return   171; }   # 0xC7
    if (i ==  72) { return   187; }   # 0xC8
    if (i ==  73) { return  8230; }   # 0xC9
    if (i ==  74) { return   160; }   # 0xCA
    if (i ==  75) { return   192; }   # 0xCB
    if (i ==  76) { return   195; }   # 0xCC
    if (i ==  77) { return   213; }   # 0xCD
    if (i ==  78) { return   338; }   # 0xCE
    if (i ==  79) { return   339; }   # 0xCF
    if (i ==  80) { return  8211; }   # 0xD0
    if (i ==  81) { return  8212; }   # 0xD1
    if (i ==  82) { return  8220; }   # 0xD2
    if (i ==  83) { return  8221; }   # 0xD3
    if (i ==  84) { return  8216; }   # 0xD4
    if (i ==  85) { return  8217; }   # 0xD5
    if (i ==  86) { return   247; }   # 0xD6
    if (i ==  87) { return  9674; }   # 0xD7
    if (i ==  88) { return   255; }   # 0xD8
    if (i ==  89) { return   376; }   # 0xD9
    if (i ==  90) { return  8260; }   # 0xDA
    if (i ==  91) { return  8364; }   # 0xDB
    if (i ==  92) { return  8249; }   # 0xDC
    if (i ==  93) { return  8250; }   # 0xDD
    if (i ==  94) { return 64257; }   # 0xDE
    if (i ==  95) { return 64258; }   # 0xDF
    if (i ==  96) { return  8225; }   # 0xE0
    if (i ==  97) { return   183; }   # 0xE1
    if (i ==  98) { return  8218; }   # 0xE2
    if (i ==  99) { return  8222; }   # 0xE3
    if (i == 100) { return  8240; }   # 0xE4
    if (i == 101) { return   194; }   # 0xE5
    if (i == 102) { return   202; }   # 0xE6
    if (i == 103) { return   193; }   # 0xE7
    if (i == 104) { return   203; }   # 0xE8
    if (i == 105) { return   200; }   # 0xE9
    if (i == 106) { return   205; }   # 0xEA
    if (i == 107) { return   206; }   # 0xEB
    if (i == 108) { return   207; }   # 0xEC
    if (i == 109) { return   204; }   # 0xED
    if (i == 110) { return   211; }   # 0xEE
    if (i == 111) { return   212; }   # 0xEF
    if (i == 112) { return 63743; }   # 0xF0
    if (i == 113) { return   210; }   # 0xF1
    if (i == 114) { return   218; }   # 0xF2
    if (i == 115) { return   219; }   # 0xF3
    if (i == 116) { return   217; }   # 0xF4
    if (i == 117) { return   305; }   # 0xF5
    if (i == 118) { return   710; }   # 0xF6
    if (i == 119) { return   732; }   # 0xF7
    if (i == 120) { return   175; }   # 0xF8
    if (i == 121) { return   728; }   # 0xF9
    if (i == 122) { return   729; }   # 0xFA
    if (i == 123) { return   730; }   # 0xFB
    if (i == 124) { return   184; }   # 0xFC
    if (i == 125) { return   733; }   # 0xFD
    if (i == 126) { return   731; }   # 0xFE
    if (i == 127) { return   711; }   # 0xFF
    return 0 - 1;
}
